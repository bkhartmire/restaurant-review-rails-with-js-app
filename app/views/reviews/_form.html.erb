<%= form_for(review) do |f| %>
  <%= render partial: 'validation_errors', locals: {review: @review} %>
  <%= f.hidden_field :restaurant_id %>
  <%= f.label :rating %>
  <%= f.number_field :rating, in: 1...6 %><br>
  <%= f.text_area :content%><br>
  <%= f.submit "Submit"%>
<% end %>
