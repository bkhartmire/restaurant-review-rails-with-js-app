<h1>This is the countries#index page. yayyy</h1>
<% @countries.each do |country| %>
  <ul>
     <h3><%= link_to country.name, country_path(country) %> (<%= country.restaurants.count%>)</h3>

  </ul>
<% end %>
<br>
